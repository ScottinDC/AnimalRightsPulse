name: Update Trends

on:
  workflow_dispatch:
    inputs:
      browse_ai_payload:
        description: "Optional Browse.ai Reddit payload JSON"
        required: false
        type: string
  repository_dispatch:
    types:
      - browse_ai_reddit
  schedule:
    - cron: "0 */3 * * *"

permissions:
  contents: write

jobs:
  update-trends:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Write Browse.ai payload
        if: ${{ github.event_name == 'repository_dispatch' || inputs.browse_ai_payload != '' }}
        env:
          BROWSE_AI_PAYLOAD: ${{ github.event_name == 'repository_dispatch' && toJson(github.event.client_payload) || inputs.browse_ai_payload }}
        run: npm run write:browse-ai-payload

      - name: Fetch Search Console data
        env:
          GSC_CLIENT_ID: ${{ secrets.GSC_CLIENT_ID }}
          GSC_CLIENT_SECRET: ${{ secrets.GSC_CLIENT_SECRET }}
          GSC_REFRESH_TOKEN: ${{ secrets.GSC_REFRESH_TOKEN }}
          GSC_SITE_A: ${{ secrets.GSC_SITE_A }}
          GSC_SITE_B: ${{ secrets.GSC_SITE_B }}
        run: npm run fetch:gsc

      - name: Fetch GA4 site search
        env:
          GA4_PROPERTY_ID_SITE_A: ${{ secrets.GA4_PROPERTY_ID_SITE_A }}
          GA4_PROPERTY_ID_SITE_B: ${{ secrets.GA4_PROPERTY_ID_SITE_B }}
          GA4_CLIENT_EMAIL: ${{ secrets.GA4_CLIENT_EMAIL }}
          GA4_PRIVATE_KEY: ${{ secrets.GA4_PRIVATE_KEY }}
        run: npm run fetch:ga4-site-search

      - name: Fetch Reddit data
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
          BROWSE_AI_REDDIT_FILE: ${{ (github.event_name == 'repository_dispatch' || inputs.browse_ai_payload != '') && 'public/data/browse-ai-reddit.json' || '' }}
        run: npm run fetch:reddit

      - name: Fetch Google Trends
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
        run: npm run fetch:google-trends

      - name: Fetch Google News
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
        run: npm run fetch:google-news

      - name: Normalize data
        run: npm run normalize:data

      - name: Build summary
        run: npm run build:summary

      - name: Commit data updates
        run: |
          if [[ -n "$(git status --porcelain public/data)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add public/data
            git commit -m "chore: refresh trend data"
            git push
          else
            echo "No data changes to commit."
          fi
